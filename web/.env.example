# Public base URL of the deployed Render backend. Must be prefixed NEXT_PUBLIC_
# so it is inlined into the client bundle. Set this in Vercel project settings.
NEXT_PUBLIC_API_BASE_URL=https://naik-api.onrender.com
