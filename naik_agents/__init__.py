"""Naik agents package.

Block 4+ populates this with the four agents (diagnostic, wealth, insurance,
compliance). Block 3 serialises the pricing kernel into ``agents/pricing/``.
All agents import their I/O contracts from ``api.schemas``.
"""
